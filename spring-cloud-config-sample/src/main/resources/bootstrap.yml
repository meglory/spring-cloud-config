spring:
  application:
    name: cloud-config
  cloud:
    config:
      uri: http://localhost:8888/
      user: mgh
      password: mgh123
      profile: dev
      label: master

endpoints:
  refresh:
    enabled: true
    sensitive: false